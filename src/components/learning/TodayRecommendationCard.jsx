// Compatibility wrapper: the dashboard now renders the unified Daily Journey card.
// Keep this export so older imports continue to work during the v2 migration.
import TodayJourneyCard from './TodayJourneyCard.jsx';

export default TodayJourneyCard;
