// Compatibility export for older imports. BIG-UPDATE-12 removed preview-only
// language state; this now points to the canonical persisted control.
export { default } from './ShimeLanguageSwitch.jsx';
