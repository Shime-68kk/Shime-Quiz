name: Playwright E2E Smoke

on:
  pull_request:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  e2e-smoke:
    name: E2E smoke
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Pin stable npm
        run: |
          npm install -g npm@9.9.4
          npm --version

      - name: Guard public npm registry
        run: |
          ! grep -R "applied-caas\|artifactory\|internal.api.openai\|packages.applied" package-lock.json package.json .npmrc 2>/dev/null
          npm config set registry https://registry.npmjs.org/
          npm config get registry

      - name: Install dependencies
        timeout-minutes: 20
        env:
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: '1'
        run: |
          set -e
          npm ci --include=dev --ignore-scripts --no-audit --no-fund --progress=false --loglevel=notice || {
            echo "npm ci failed; retrying once with npm install fallback"
            find "$HOME/.npm/_logs" -type f -name "*debug*.log" -print -exec tail -80 {} \; || true
            rm -rf node_modules
            npm install --include=dev --ignore-scripts --no-audit --no-fund --progress=false --loglevel=notice
          }

      - name: Verify installed tooling
        run: |
          test -x node_modules/.bin/vite
          test -x node_modules/.bin/vitest

      # Phase 25H validator retained as historical reference; not run as Phase 25I merge-blocking gate.
      # node scripts/validate-phase25h-backup-health-persistence-signal-design.js

      # Phase 25I validator retained as historical reference; not run as Phase 25J merge-blocking gate.
      # node scripts/validate-phase25i-backup-health-thin-read-only-signal-layer.js

      # Phase 25J validator retained as historical reference; not run as Phase 25K merge-blocking gate.
      # node scripts/validate-phase25j-backup-health-read-only-integration-design-gate.js

      # Phase 25K validator retained as historical reference; not run as Phase 25L merge-blocking gate.
      # node scripts/validate-phase25k-backup-health-test-only-default-off-integration-prototype.js

      # Phase 25L validator retained as historical reference; not run as Phase 25M merge-blocking gate.
      # node scripts/validate-phase25l-backup-health-production-ui-design-gate.js

      # Phase 25M validator retained as historical reference; not run as Phase 25N merge-blocking gate.
      # node scripts/validate-phase25m-backup-health-limited-default-off-ui-prototype.js

      # Phase 25N validator retained as historical reference; not run as Phase 26A merge-blocking gate.
      # node scripts/validate-phase25n-backup-health-evidence-closure-gate.js

      # Phase 26A validator retained as historical reference; not run as Phase 26B merge-blocking gate.
      # node scripts/validate-phase26a-local-first-hybrid-readiness-direction.js

      # Phase 26B validator retained as historical reference; not run as Phase 26C merge-blocking gate.
      # node scripts/validate-phase26b-local-first-hybrid-evidence-redecision.js

      # Phase 26C validator retained as historical reference; not run as Phase 26D merge-blocking gate.
      # node scripts/validate-phase26c-limited-default-off-ui-wiring-design.js

      # Phase 26D validator retained as historical reference; not run as Phase 26E merge-blocking gate.
      # node scripts/validate-phase26d-limited-default-off-ui-wiring-prototype.js

      # Phase 26D-HF1 validator retained as historical reference; not run as Phase 26E merge-blocking gate.
      # node scripts/validate-phase26d-hf1-restore-actions-checkout-permission.js

      # Phase 26E validator retained as historical reference; not run as Phase 27A merge-blocking gate.
      # node scripts/validate-phase26e-tester-evidence-ui-wiring-redecision.js

      # Phase 27A validator retained as historical reference; not run as Phase 27B merge-blocking gate.
      # node scripts/validate-phase27a-backup-restore-adapter-awareness-design.js

      # Phase 27B validator retained as historical reference; not run as Phase 27C merge-blocking gate.
      # node scripts/validate-phase27b-adapter-awareness-evidence-runtime-design.js

      # Phase 27C validator retained as historical reference; not run as Phase 27D merge-blocking gate.
      # node scripts/validate-phase27c-test-only-adapter-awareness-model.js

      # Phase 27D validator retained as historical reference; not run as Phase 27E merge-blocking gate.
      # node scripts/validate-phase27d-adapter-awareness-model-evidence-integration-design.js

      # Phase 27E validator retained as historical reference; not run as Phase 27F merge-blocking gate.
      # node scripts/validate-phase27e-thin-read-only-adapter-awareness-integration-prototype.js

      # Phase 27F validator retained as historical reference; not run as Phase 28A merge-blocking gate.
      # node scripts/validate-phase27f-adapter-awareness-integration-evidence-closure.js

      # Phase 28A validator retained as historical reference; not run as Phase 28B merge-blocking gate.
      # node scripts/validate-phase28a-generated-test-restore-rehearsal-design.js

      # Phase 28B validator retained as historical reference; not run as Phase 28C merge-blocking gate.
      # node scripts/validate-phase28b-test-only-restore-rehearsal-planner.js

      # Phase 28C validator retained as historical reference; not run as Phase 28D merge-blocking gate.
      # node scripts/validate-phase28c-restore-rehearsal-planner-evidence-prototype-design.js

      # Phase 28D validator retained as historical reference; not run as Phase 28E merge-blocking gate.
      # node scripts/validate-phase28d-generated-test-restore-rehearsal-prototype.js

      # Phase 28E validator retained as historical reference; not run as Phase 29A merge-blocking gate.
      # node scripts/validate-phase28e-generated-test-restore-rehearsal-evidence-closure.js

      # Phase 29A validator retained as historical reference; not run as Phase 29B merge-blocking gate.
      # node scripts/validate-phase29a-local-first-hybrid-readiness-evidence-redecision.js

      # Phase 29B validator retained as historical reference; not run as Phase 29C merge-blocking gate.
      # node scripts/validate-phase29b-beta-evidence-gate-planning.js

      # Phase 29C validator retained as historical reference; not run as Phase 29D merge-blocking gate.
      # node scripts/validate-phase29c-generated-test-manual-browser-evidence-run.js

      # Phase 29D validator retained as historical reference; not run as Phase 29E merge-blocking gate.
      # node scripts/validate-phase29d-evidence-packet-review-beta-gate-redecision.js

      # Phase 29E validator retained as historical reference; not run as Phase 29F merge-blocking gate.
      # node scripts/validate-phase29e-targeted-missing-evidence-collection.js

      # Phase 29F validator retained as historical reference; not run as Phase 30A merge-blocking gate.
      # node scripts/validate-phase29f-evidence-review-limited-beta-candidate-redecision.js

      # Phase 30A validator retained as historical reference; not run as Phase 30B merge-blocking gate.
      # node scripts/validate-phase30a-limited-beta-candidate-claim-copy-boundary-audit.js

      # Phase 30B validator retained as historical reference; not run as Phase 30C merge-blocking gate.
      # node scripts/validate-phase30b-limited-beta-candidate-gate.js

      # Phase 30C validator retained as historical reference; not run as Phase 31A merge-blocking gate.
      # node scripts/validate-phase30c-beta-ready-decision-hold.js

      # Phase 31A validator retained as historical reference; not run as Phase 31B merge-blocking gate.
      # node scripts/validate-phase31a-post-limited-beta-roadmap-data-safety-ux-planning.js

      # Phase 31B validator retained as historical reference; not run as Phase 31C merge-blocking gate.
      # node scripts/validate-phase31b-data-safety-ux-design-gate.js

      # Phase 31C validator retained as historical reference; not run as Phase 31D merge-blocking gate.
      # node scripts/validate-phase31c-data-safety-ux-prototype.js

      # Phase 31D validator retained as historical reference; not run as Phase 31E merge-blocking gate.
      # node scripts/validate-phase31d-data-safety-ux-evidence-review.js

      # Phase 31E validator retained as historical reference; not run as Phase 31F merge-blocking gate.
      # node scripts/validate-phase31e-data-safety-ux-controlled-visibility-gate.js

      # Phase 31F validator retained as historical reference; not run as Phase 31G merge-blocking gate.
      # node scripts/validate-phase31f-data-safety-ux-internal-visibility-gate.js

      # Phase 31G validator retained as historical reference; not run as Phase 31H merge-blocking gate.
      # node scripts/validate-phase31g-data-safety-ux-internal-visibility-implementation.js

      # Phase 31H validator retained as historical reference; not run as Phase 31I merge-blocking gate.
      # node scripts/validate-phase31h-data-safety-ux-internal-visibility-evidence-review.js

      # Phase 31I validator retained as historical reference; not run as Phase 31J merge-blocking gate.
      # node scripts/validate-phase31i-data-safety-ux-internal-browser-evidence.js

      # Phase 31J validator retained as historical reference; not run as Phase 32A merge-blocking gate.
      # node scripts/validate-phase31j-data-safety-ux-visibility-redecision.js

      # Phase 32A validator retained as historical reference; not run as Phase 32B merge-blocking gate.
      # node scripts/validate-phase32a-beta-ready-remaining-evidence-reentry.js

      # Phase 32B validator retained as historical reference; not run as Phase 32C merge-blocking gate.
      # node scripts/validate-phase32b-remaining-evidence-collection.js

      # Phase 32C validator retained as historical reference; not run as Phase 32D merge-blocking gate.
      # node scripts/validate-phase32c-remaining-evidence-review.js

      # Phase 32D validator retained as historical reference; not run as Phase 32E merge-blocking gate.
      # node scripts/validate-phase32d-claim-copy-cleanup.js

      # Phase 32E validator retained as historical reference; not run as Phase 32F merge-blocking gate.
      # node scripts/validate-phase32e-beta-ready-redecision-input-review.js

      # Phase 32F validator retained as historical reference; not run as Phase 33A merge-blocking gate.
      # node scripts/validate-phase32f-beta-ready-redecision.js

      # Phase 33A validator retained as historical reference; not run as Phase 33B merge-blocking gate.
      # node scripts/validate-phase33a-limited-beta-candidate-stabilization.js

      # Phase 33B validator retained as historical reference; not run as Phase 33C merge-blocking gate.
      # node scripts/validate-phase33b-controlled-limited-beta-prep.js

      # Phase 33C validator retained as historical reference; not run as Phase 33D merge-blocking gate.
      # node scripts/validate-phase33c-controlled-limited-beta-prep-review.js

      # Phase 33D validator retained as historical reference; not run as Phase 33E merge-blocking gate.
      # node scripts/validate-phase33d-limited-beta-candidate-release-notes-review.js

      # Phase 33E validator retained as historical reference; not run as Phase 33F merge-blocking gate.
      # node scripts/validate-phase33e-controlled-limited-beta-release-package-review.js

      # Phase 33F validator retained as historical reference; not run as Phase 34A merge-blocking gate.
      # node scripts/validate-phase33f-controlled-limited-beta-final-go-no-go.js

      # Phase 34A validator retained as historical reference; not run as Phase 34B merge-blocking gate.
      # node scripts/validate-phase34a-leader-ui-effects-design-gate.js

      # Phase 34B validator retained as historical reference; not run as Phase 34C merge-blocking gate.
      # node scripts/validate-phase34b-leader-ui-effects-implementation.js

      # Phase 34C validator retained as historical reference; not run as Phase 35A merge-blocking gate.
      # node scripts/validate-phase34c-leader-ui-effects-evidence-review.js

      # Phase 35A validator retained as historical reference; not run as Phase 35B merge-blocking gate.
      # node scripts/validate-phase35a-leader-ui-structural-scope-gate.js

      # Phase 35B validator retained as historical reference; not run as Phase 35C merge-blocking gate.
      # node scripts/validate-phase35b-library-bookshelf.js

      # Phase 35C validator retained as historical reference; not run as Phase 35D merge-blocking gate.
      # node scripts/validate-phase35c-library-bookshelf-evidence-review.js

      # Phase 35D validator retained as historical reference; not run as Phase 35E merge-blocking gate.
      # node scripts/validate-phase35d-dashboard-deconstruction-research-scope.js

      # Phase 35E validator retained as historical reference; not run as Phase 35F merge-blocking gate.
      # node scripts/validate-phase35e-dashboard-calm-home.js

      - name: Validate Phase 35F Dashboard Calm Home Evidence Review
        run: node scripts/validate-phase35f-dashboard-calm-home-evidence-review.js

      - name: Build app
        run: npm run build

      - name: Run Vitest unit tests
        run: npm run test:unit

      - name: Validate smoke fixture
        run: node scripts/validate-smoke-fixture.js

      # Historical Phase 15/16 audit references retained for legacy workflow-registration tests.
      # These are intentionally not run by the default e2e-smoke PR gate.
      # node scripts/validate-phase15e-controlled-internal-activation-harness.js
      # node scripts/validate-phase15f-studyroom-copy-ux-alignment.js
      # node scripts/validate-phase15h-fsrs-foundation-closure-phase16-readiness.js
      # node scripts/validate-phase16a-vietnamese-first-ux-copy-alignment.js
      # node scripts/validate-phase16b-hybrid-local-first-optional-sync-direction.js
      # node scripts/validate-phase16c-storage-large-import-edugen-risk-audit.js
      # node scripts/validate-phase16d-shime-study-identity-product-principles.js
      # node scripts/validate-phase16e-visual-polish-quick-wins.js
      # node scripts/validate-phase16f-edugen-draft-workshop-connector-foundation.js
      # node scripts/validate-phase16g-edugen-draft-review-import-flow.js
      # node scripts/validate-phase16h-edugen-draft-quality-review-source-aware-library.js

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Run Playwright E2E smoke
        run: npm run test:e2e:smoke

      - name: Run onboarding E2E smoke
        run: npm run test:e2e:onboarding

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          if-no-files-found: ignore

      - name: Upload Playwright test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-test-results
          path: test-results/
          if-no-files-found: ignore
