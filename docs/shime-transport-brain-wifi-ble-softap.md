# Shime Transport Brain

Wi-Fi/WebSocket LAN is the primary future live channel when safe and same-LAN.

BLE is for provisioning, presence, tiny capsule fallback, and pairing. SoftAP is setup/recovery. USB Serial is dev/debug. ESP-NOW is future accessory-swarm only, not the primary app channel.

Phase 34X implements recommendations only. It opens no connection.
