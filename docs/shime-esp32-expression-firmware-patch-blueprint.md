# Shime ESP32 Expression Firmware Patch Blueprint

This blueprint describes a future Phase 42 patch only. It must remain log-only parser work.

Allowed future scope: isolated parser changes in protocol files, bounded newline JSON input, required-field checks, nested forbidden-key scan, expression family/channel validation, and ACCEPT/REJECT log output.

Forbidden future scope: motion, motor/servo behavior, Wi-Fi/BLE transport, secret material, app runtime dependency, schedule mutation, and raw quiz content.

