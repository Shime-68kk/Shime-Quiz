# Shime Expression Golden Fixtures

Golden fixtures provide host-side protocol examples for future parser tests.

They include all valid expression families plus invalid envelopes for secret material, unlocked motion, unsafe send status, non-dry-run state, unsupported protocol version, malformed JSON, and forbidden channels.

Valid fixtures must not contain raw quiz content. Invalid fixtures are for parser rejection tests only.

