# Cognitive Companion V2 Integration Plan

This is the safe future path for V2. Phase 32 does not perform integration.

1. V2 review only.
2. V2 Control Center dev-only toggle.
3. V2 fake scenario panel.
4. V2 live observe-only panel.
5. V2 dry-run command preview.
6. V2 fake server dry-run transcript.
7. ESP32 log-only mapping.
8. Expression-only robot prototype.
9. No motion until a separate safety phase.

## Constraints

- No StudyRoom behavior change until an explicit integration phase.
- No DeviceBridge runtime behavior change in review phases.
- No storage, network, telemetry, cloud, or AI API.
- No robot command sending.
- No raw quiz payload in V2 state, memory, audit, benchmark, golden replay, or readiness reports.
- UI must remain dev-only and disabled by default when V2 is eventually surfaced.
