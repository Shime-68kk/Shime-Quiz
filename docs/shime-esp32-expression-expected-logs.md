# Shime ESP32 Expression Expected Logs

Expected logs define future parser output for expression envelopes.

Valid fixtures should produce ACCEPT log-only lines. Invalid fixtures should produce REJECT log-only lines. The only motion-related log allowed is an explicit locked/no-motion safety statement.

No firmware was changed in Phase 41X.

