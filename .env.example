# Optional EduGen File Processor endpoint.
# Run EduGen separately; Shime uses it only to extract document text into a draft preview.
# Phase 7E supports PDF, DOCX, PPTX, and ZIP through EduGen. No OCR or AI generation is included.
VITE_FILE_PROCESSOR_URL=http://localhost:3333
